import { StyleSheet, Text, View } from 'react-native'
import React from 'react'
import ListModule from './courses/ListModule'

const Courses = () => {
  return (
    <View>
      <ListModule/>
    </View>
  )
}

export default Courses

const styles = StyleSheet.create({})